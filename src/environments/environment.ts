// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCGrewB8m6vsSrbqHH45Vw7Opx2V1_aONU",
    authDomain: "dota2counterpick.firebaseapp.com",
    databaseURL: "https://dota2counterpick-default-rtdb.firebaseio.com",
    projectId: "dota2counterpick",
    storageBucket: "dota2counterpick.appspot.com",
    messagingSenderId: "237601992244",
    appId: "1:237601992244:web:d85dee73a83e73e5e3a461",
    measurementId: "G-VEFQW2EM3Y"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
